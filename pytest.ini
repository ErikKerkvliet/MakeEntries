[pytest]
markers =
    browser: integration tests that require a real Chrome browser and internet access

# Default test run (pytest) only collects unit tests.
# Run browser tests explicitly: pytest tests/browser/ -v
testpaths = tests
